apiVersion: v1
kind: Secret
metadata:
  name: netflix-secrets
  namespace: netflix-clone
type: Opaque
stringData:
  jwt-secret: "your_production_jwt_secret_change_this_in_production"
  # Add other secrets as needed
  # mongodb-uri: "mongodb://username:password@mongodb:27017/netflix_clone"
